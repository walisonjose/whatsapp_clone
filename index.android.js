import React from 'react';
import { AppRegistry } from 'react-native';
import App from './src/App';

const whatsapp_demo = props => (
  <App />
)

AppRegistry.registerComponent('whatsapp_demo', () => whatsapp_demo);
